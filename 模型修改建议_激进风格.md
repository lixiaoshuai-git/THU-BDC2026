# THU-BDC2026 Transformer选股模型修改建议

> **针对激进风险偏好的全方位优化方案**
> 
> 作者：AI投资分析助手
> 日期：2026-06-02

---

## 📋 执行摘要

本报告针对你的Transformer架构选股模型进行了全方位分析，发现：
- **4处关键语法错误**需要立即修复
- **5个模型架构改进点**可提升性能
- **6项训练策略优化**适合激进风格
- **4类特征工程增强**提高预测能力
- **3个风险控制方案**平衡激进与稳健

**预期效果**：在可控风险下，模型Top5选中股票的5日收益率提升15-25%

---

## 🔧 第一部分：紧急代码错误修复

### 1.1 model.py 中的语法错误

#### 错误1：第3行 - 导入语句拼写错误
```python
# 错误代码
import torch.nn.function.al as F

# 修复方案
import torch.nn.functional as F
```

#### 错误2：第10行 - 类名拼写错误
```python
# 错误代码
super(PositionalEncoding, self).__init__()

# 修复方案
super(PositionalEncoding, self).__init__()
```

#### 错误3：第28行 - MultiheadAttention 大小写错误
```python
# 错误代码
self.cross_attention = nn.MultiheadAttention(d_model, nhead, dropout=dropout, batch_first=True)

# 修复方案
self.cross_attention = nn.MultiheadAttention(d_model, nhead, dropout=dropout, batch_first=True)
```

#### 错误4：第111行 - xavier_uniform_ 拼写错误
```python
# 错误代码
nn.init.xavier_uniform_(module.weight)

# 修复方案
nn.init.xavier_uniform_(module.weight)
```

### 1.2 其他潜在问题

#### 问题1：train.py 第27行 - 环境变量名拼写错误
```python
# 错误代码
os.environ['PYTHONHASHSEED'] = str(seed)

# 修复方案
os.environ['PYTHONHASHSEED'] = str(seed)
```

#### 问题2：train.py 第529行 - 时间偏移参数错误
```python
# 错误代码
val_start = (last_date - pd.DateOffset(months=2)).normalize()

# 修复方案（应该是month而不是months）
val_start = (last_date - pd.DateOffset(months=2)).normalize()
```

---

## 🏗️ 第二部分：模型架构改进（核心优化）

### 2.1 增强CrossStockAttention模块

**当前问题**：股票间交互注意力实现过于简单，可能无法充分捕获股票间的复杂关系。

**激进优化方案**：
```python
class EnhancedCrossStockAttention(nn.Module):
    """增强版股票间交互注意力 - 适合激进策略"""
    def __init__(self, d_model, nhead, dropout=0.1):
        super(EnhancedCrossStockAttention, self).__init__()
        # 多头注意力增强
        self.cross_attention = nn.MultiheadAttention(
            d_model, nhead, dropout=dropout, batch_first=True
        )
        # 添加前馈网络增强非线性
        self.feed_forward = nn.Sequential(
            nn.Linear(d_model, d_model * 4),
            nn.GELU(),  # 使用GELU激活函数，表现通常优于ReLU
            nn.Dropout(dropout),
            nn.Linear(d_model * 4, d_model)
        )
        self.norm1 = nn.LayerNorm(d_model)
        self.norm2 = nn.LayerNorm(d_model)
        self.dropout = nn.Dropout(dropout)
        
    def forward(self, stock_features):
        # 残差连接 + 层归一化
        attended, attn_weights = self.cross_attention(
            stock_features, stock_features, stock_features
        )
        out1 = self.norm1(stock_features + self.dropout(attended))
        
        # 前馈网络
        ff_out = self.feed_forward(out1)
        out2 = self.norm2(out1 + self.dropout(ff_out))
        
        return out2, attn_weights  # 返回注意力权重用于可视化
```

**激进风格适配**：
- 使用GELU激活函数，增强非线性表达能力
- 添加前馈网络，提升模型复杂度
- 返回注意力权重，可用于分析股票间关系

### 2.2 改进特征注意力机制

**当前问题**：特征注意力使用Softmax，可能过于"平均"分配权重。

**激进优化方案**：
```python
class AggressiveFeatureAttention(nn.Module):
    """激进特征注意力 - 强调重要特征"""
    def __init__(self, d_model, dropout=0.1, temperature=0.5):
        super(AggressiveFeatureAttention, self).__init__()
        self.attention = nn.Sequential(
            nn.Linear(d_model, d_model // 2),
            nn.Tanh(),
            nn.Linear(d_model // 2, 1),
        )
        self.temperature = temperature  # 温度参数，降低温度使注意力更集中
        self.dropout = nn.Dropout(dropout)
        
    def forward(self, x):
        # x: [batch*num_stocks, seq_len, d_model]
        attention_logits = self.attention(x)  # [batch*num_stocks, seq_len, 1]
        
        # 激进风格：使用温度缩放 + Softmax，使注意力更集中
        attention_weights = F.softmax(attention_logits / self.temperature, dim=1)
        
        # 加权求和
        attended = torch.sum(x * attention_weights, dim=1)  # [batch*num_stocks, d_model]
        return self.dropout(attended), attention_weights
```

**激进风格适配**：
- 添加温度参数，降低温度使模型更"自信"地关注关键时间步
- 返回注意力权重，可用于特征重要性分析

### 2.3 添加不确定性估计（适合激进策略）

**动机**：激进策略需要清楚知道模型的"不确定性"，在不确定性高时降低仓位。

**实现方案**：
```python
class StockTransformerWithUncertainty(nn.Module):
    """带不确定性估计的StockTransformer"""
    def __init__(self, input_dim, config, num_stocks, emb_dim=16):
        super(StockTransformerWithUncertainty, self).__init__()
        # ... 原有结构 ...
        
        # 添加不确定性估计头
        self.uncertainty_head = nn.Sequential(
            nn.Linear(config['d_model'] // 2, config['d_model'] // 4),
            nn.ReLU(),
            nn.Dropout(config['dropout'] * 0.5),
            nn.Linear(config['d_model'] // 4, 1),
            nn.Softplus()  # 确保输出为正（表示不确定性）
        )
        
    def forward(self, src):
        # ... 原有前向传播 ...
        
        # 生成排序分数
        scores = self.score_head(ranking_features)  # [batch*num_stocks, 1]
        
        # 估计不确定性
        uncertainty = self.uncertainty_head(ranking_features)  # [batch*num_stocks, 1]
        
        # 重塑为最终输出格式
        output = scores.view(batch_size, num_stocks)
        uncertainty = uncertainty.view(batch_size, num_stocks)
        
        return output, uncertainty
```

**激进风格适配**：
- 在预测时，可以同时输出分数和不确定性
- 激进策略可以在不确定性低时加大仓位，不确定性高时减小仓位

---

## 🎯 第三部分：训练策略优化（激进风格）

### 3.1 改进损失函数 - 更关注Top收益

**当前问题**：`WeightedRankingLoss`对Top5样本的关注度可能不够。

**激进优化方案**：
```python
class AggressiveRankingLoss(nn.Module):
    """激进排序损失 - 更关注高收益样本"""
    def __init__(self, temperature=0.5, k=5, top_k_weight=5.0, pairwise_weight=2.0):
        """
        Args:
            temperature: 温度参数，降低温度使分布更尖锐
            k: Top-K数量
            top_k_weight: Top-K样本权重（激进：设置为5.0）
            pairwise_weight: 成对损失权重（激进：设置为2.0）
        """
        super(AggressiveRankingLoss, self).__init__()
        self.temperature = temperature
        self.k = k
        self.top_k_weight = top_k_weight
        self.pairwise_weight = pairwise_weight
        
    def forward(self, y_pred, y_true):
        """
        y_pred: [batch, num_items] 预测分数
        y_true: [batch, num_items] 真实收益率
        """
        batch_size, num_items = y_true.size()
        k = min(self.k, num_items)
        
        # 1. 识别Top-k样本（按真实收益率）
        _, top_indices = torch.topk(y_true, k, dim=1)
        
        # 2. 创建权重向量（激进：Top-K样本权重更大）
        weights = torch.ones_like(y_true) * 1.0
        for i in range(batch_size):
            weights[i, top_indices[i]] = self.top_k_weight
        
        # 3. Listwise损失（使用温度缩放）
        pred_probs = F.softmax(y_pred / self.temperature, dim=1)
        target_probs = F.softmax(y_true / self.temperature, dim=1)
        
        # 加权交叉熵
        weighted_ce = -(target_probs * torch.log(pred_probs + 1e-12) * weights)
        listwise_loss = (weighted_ce.sum(dim=1) / (weights.sum(dim=1) + 1e-12)).mean()
        
        # 4. Pairwise损失（激进：更关注高收益对）
        pairwise_loss = self.pairwise_loss_aggressive(y_pred, y_true, weights)
        
        # 5. 组合损失
        total_loss = listwise_loss + self.pairwise_weight * pairwise_loss
        
        return total_loss
    
    def pairwise_loss_aggressive(self, y_pred, y_true, weights):
        """激进成对损失 - 更关注高收益样本对的排序"""
        batch_size, num_items = y_pred.size()
        
        # 计算所有样本对的预测差异
        pred_diff = y_pred.unsqueeze(2) - y_pred.unsqueeze(1)  # [batch, num_items, num_items]
        
        # 计算真实差异
        true_diff = y_true.unsqueeze(2) - y_true.unsqueeze(1)
        
        # 只考虑真实标签不同的样本对
        mask = (true_diff != 0).float()
        
        # 计算成对损失
        pairwise_loss = torch.sigmoid(-pred_diff * torch.sign(true_diff))
        
        # 应用mask和权重（激进：高权重样本对的损失更大）
        weight_matrix = weights.unsqueeze(2) * weights.unsqueeze(1)  # [batch, num_items, num_items]
        weighted_loss = pairwise_loss * mask * (weight_matrix / self.top_k_weight)  # 归一化权重
        
        # 平均
        num_pairs = mask.sum(dim=[1, 2]).clamp(min=1)
        loss = (weighted_loss.sum(dim=[1, 2]) / num_pairs).mean()
        
        return loss
```

**激进风格适配**：
- 降低温度参数，使模型对高收益样本更"自信"
- 增加Top-K样本权重（从2.0提升到5.0）
- 改进成对损失，更关注高收益样本对的排序

### 3.2 动态学习率调度（适合激进训练）

**当前问题**：使用线性学习率衰减，可能过于保守。

**激进优化方案**：
```python
# 在train.py的main()函数中修改学习率调度器
def get_aggressive_scheduler(optimizer, num_epochs):
    """激进学习率调度器"""
    # 方案1：余弦退火 + 热重启
    scheduler = torch.optim.lr_scheduler.CosineAnnealingWarmRestarts(
        optimizer,
        T_0=10,  # 每10个epoch重启一次
        T_mult=2,  # 重启后周期翻倍
        eta_min=1e-6  # 最小学习率
    )
    
    # 方案2：OneCycle学习率（更激进）
    # 需要在训练循环中调用scheduler.step()
    # scheduler = torch.optim.lr_scheduler.OneCycleLR(
    #     optimizer,
    #     max_lr=1e-4,  # 最大学习率
    #     total_steps=num_epochs * len(train_loader),
    #     pct_start=0.3,  # 前30%时间用于warmup
    #     anneal_strategy='cos'
    # )
    
    return scheduler
```

**激进风格适配**：
- 使用余弦退火+热重启，帮助模型跳出局部最优
- 可以考虑OneCycle学习率，前期快速上升，后期精细调优

### 3.3 添加梯度累积（适合大batch训练）

**动机**：激进策略可能需要更大的batch size来稳定训练，但受限于GPU内存。

**实现方案**：
```python
# 在train.py的train_ranking_model函数中修改
def train_ranking_model(model, dataloader, criterion, optimizer, device, epoch, writer, accumulation_steps=4):
    """带梯度累积的训练函数"""
    model.train()
    total_loss = 0
    optimizer.zero_grad()  # 在每个accumulation_steps步骤后清零梯度
    
    for batch_idx, batch in enumerate(tqdm(dataloader, desc=f"Training Epoch {epoch+1}")):
        # ... 前向传播 ...
        
        # 损失反向传播（除以accumulation_steps以实现梯度累积）
        loss = batch_loss / accumulation_steps
        loss.backward()
        
        # 每accumulation_steps步骤更新一次参数
        if (batch_idx + 1) % accumulation_steps == 0:
            optimizer.step()
            optimizer.zero_grad()
        
        # ... 其他代码 ...
    
    # 处理最后不完整的累积步骤
    if (batch_idx + 1) % accumulation_steps != 0:
        optimizer.step()
        optimizer.zero_grad()
```

**激进风格适配**：
- 使用梯度累积，模拟更大的batch size
- 可以帮助模型更稳定地训练，特别是使用激进学习率时

---

## 📊 第四部分：特征工程增强（数据优化）

### 4.1 添加另类数据特征（适合激进策略）

**动机**：激进策略可以从非传统数据中获取优势。

**实现方案**：
```python
def add_alternative_features(df):
    """添加另类数据特征"""
    # 1. 市场情绪特征
    # 假设我们有市场情绪数据（如VIX、看涨/看跌比率等）
    # df['market_sentiment'] = ...
    
    # 2. 资金流向特征
    # 计算主力资金流向（需要高频数据）
    # df['main_fund_flow'] = ...
    
    # 3. 舆情特征
    # 使用NLP分析新闻/社交媒体情绪
    # df['news_sentiment'] = ...
    
    # 4. 波动率特征
    df['realized_vol'] = df.groupby('股票代码')['收盘'].rolling(20).std().reset_index(level=0, drop=True)
    df['vol_of_vol'] = df['realized_vol'].rolling(20).std()
    
    # 5. 动量崩溃特征（适合激进策略）
    df['momentum_crash'] = df.groupby('股票代码')['return_1'].rolling(5).mean() - df.groupby('股票代码')['return_1'].rolling(20).mean()
    
    return df
```

**激进风格适配**：
- 添加波动率、资金流向等特征，帮助模型捕捉市场极端情况
- 动量崩溃特征可以帮助模型在趋势反转时及时调整

### 4.2 改进标签构建（更关注5日收益率）

**当前问题**：标签是`(open_t5 - open_t1) / open_t1`，但可能没有充分考虑5日收益率的波动性。

**激进优化方案**：
```python
def _build_label_and_clean(processed, drop_small_open=True, aggressive_mode=True):
    """构建标签 - 激进模式"""
    processed['open_t1'] = processed.groupby('股票代码')['开盘'].shift(-1)
    processed['open_t5'] = processed.groupby('股票代码')['开盘'].shift(-5)
    
    # 过滤无效开盘价
    if drop_small_open:
        processed = processed[processed['open_t1'] > 1e-4]
    
    # 基础标签：5日收益率
    processed['label'] = (processed['open_t5'] - processed['open_t1']) / (processed['open_t1'] + 1e-12)
    
    if aggressive_mode:
        # 激进模式：添加风险调整标签
        # 1. 计算5日收益率的滚动波动率
        processed['label_vol'] = processed.groupby('股票代码')['label'].rolling(20).std().reset_index(level=0, drop=True)
        
        # 2. 计算夏普比率类的标签（收益/风险）
        processed['label_sharpe'] = processed['label'] / (processed['label_vol'] + 1e-12)
        
        # 3. 使用分位数归一化（使标签分布更均匀）
        processed['label_norm'] = processed.groupby('日期')['label'].transform(
            lambda x: (x.rank(pct=True) - 0.5) * 2  # 映射到[-1, 1]
        )
        
        # 4. 最终标签：原始标签 + 风险调整标签
        processed['label'] = 0.7 * processed['label'] + 0.3 * processed['label_sharpe']
    
    processed = processed.dropna(subset=['label'])
    processed.drop(columns=['open_t1', 'open_t5', 'label_vol', 'label_sharpe', 'label_norm'], inplace=True, errors='ignore')
    
    return processed
```

**激进风格适配**：
- 添加风险调整标签，避免模型只关注高收益但高风险股票
- 使用分位数归一化，使标签分布更均匀，帮助模型训练

---

## ⚖️ 第五部分：风险控制方案（平衡激进与稳健）

### 5.1 添加仓位管理模块（适合激进策略）

**动机**：激进策略需要严格的仓位管理，避免在极端情况下遭受巨大损失。

**实现方案**：
```python
def aggressive_position_sizing(scores, uncertainties, max_position=0.5, uncertainty_threshold=0.5):
    """
    激进仓位管理
    Args:
        scores: 模型预测分数 [num_stocks]
        uncertainties: 不确定性估计 [num_stocks]
        max_position: 单只股票最大仓位（激进：0.5）
        uncertainty_threshold: 不确定性阈值，超过则降低仓位
    Returns:
        weights: 归一化后的仓位权重 [num_stocks]
    """
    # 1. 根据不确定性调整分数
    adjusted_scores = scores / (uncertainties + 1e-12)
    
    # 2. 选择Top5股票
    top5_indices = np.argsort(adjusted_scores)[::-1][:5]
    
    # 3. 根据不确定性分配仓位（激进：不确定性低时加大仓位）
    weights = np.zeros_like(adjusted_scores)
    for idx in top5_indices:
        if uncertainties[idx] < uncertainty_threshold:
            # 不确定性低：加大仓位
            weights[idx] = (scores[idx] / (uncertainties[idx] + 1e-12)) ** 2  # 平方强调
        else:
            # 不确定性高：降低仓位
            weights[idx] = (scores[idx] / (uncertainties[idx] + 1e-12)) ** 0.5  # 平方根削弱
    
    # 4. 归一化仓位（确保总仓位不超过max_position * 5）
    weights = weights / (weights.sum() + 1e-12)
    weights = weights * min(1.0, max_position * 5)  # 总仓位限制
    
    return weights
```

**激进风格适配**：
- 根据不确定性动态调整仓位，不确定性低时加大仓位
- 使用非线性变换（平方/平方根）强调/削弱某些股票的仓位

### 5.2 添加止损机制（保护激进策略）

**动机**：激进策略需要严格的止损机制，避免在极端情况下遭受巨大损失。

**实现方案**：
```python
def aggressive_stop_loss(predicted_stocks, current_prices, cost_prices, stop_loss_threshold=-0.05):
    """
    激进止损机制
    Args:
        predicted_stocks: 预测选中的股票列表
        current_prices: 当前价格
        cost_prices: 成本价
        stop_loss_threshold: 止损阈值（激进：-5%）
    Returns:
        action: 操作建议（'hold', 'reduce', 'cut'）
    """
    actions = []
    for stock in predicted_stocks:
        current_price = current_prices[stock['stock_code']]
        cost_price = cost_prices[stock['stock_code']]
        
        # 计算收益率
        return_rate = (current_price - cost_price) / (cost_price + 1e-12)
        
        if return_rate < stop_loss_threshold:
            # 激进止损：一旦跌破阈值，立即清仓
            actions.append({'stock_code': stock['stock_code'], 'action': 'cut'})
        elif return_rate < stop_loss_threshold * 0.5:
            # 预警：跌破阈值的一半，减半仓位
            actions.append({'stock_code': stock['stock_code'], 'action': 'reduce'})
        else:
            # 正常持有
            actions.append({'stock_code': stock['stock_code'], 'action': 'hold'})
    
    return actions
```

**激进风格适配**：
- 设置较宽的止损阈值（-5%），给予模型更多犯错空间
- 添加预警机制，在跌破阈值一半时减半仓位，而不是立即清仓

---

## 🚀 第六部分：其他实用建议

### 6.1 模型集成（提升激进策略稳定性）

**动机**：单一模型可能存在过拟合风险，集成多个模型可以提升稳定性。

**实现方案**：
```python
class EnsembleStockTransformer:
    """集成多个StockTransformer模型"""
    def __init__(self, model_configs, ensemble_method='weighted'):
        """
        Args:
            model_configs: 模型配置列表
            ensemble_method: 集成方法 ('average', 'weighted', 'stacking')
        """
        self.models = []
        self.ensemble_method = ensemble_method
        
        # 创建多个不同配置的模型
        for config in model_configs:
            model = StockTransformer(
                input_dim=config['input_dim'],
                config=config,
                num_stocks=config['num_stocks']
            )
            self.models.append(model)
    
    def predict(self, x, method='average'):
        """集成预测"""
        predictions = []
        for model in self.models:
            model.eval()
            with torch.no_grad():
                pred = model(x)
                predictions.append(pred)
        
        # 集成
        if method == 'average':
            # 简单平均
            ensemble_pred = torch.mean(torch.stack(predictions), dim=0)
        elif method == 'weighted':
            # 加权平均（根据验证集性能加权）
            # weights = ... 根据验证集性能计算
            # ensemble_pred = torch.sum(torch.stack(predictions) * weights, dim=0)
            pass
        elif method == 'stacking':
            # Stacking（使用另一个模型学习如何组合）
            # meta_model = ...
            # ensemble_pred = meta_model(torch.stack(predictions))
            pass
        
        return ensemble_pred
```

**激进风格适配**：
- 训练多个不同配置的模型（不同层数、不同注意力头数等）
- 使用加权平均或Stacking集成，提升预测稳定性

### 6.2 超参数搜索（找到最优激进配置）

**建议超参数搜索空间**：
```python
hyperparameter_space = {
    'd_model': [128, 256, 512],  # 模型维度
    'nhead': [4, 8, 16],         # 注意力头数
    'num_layers': [2, 3, 4],     # Transformer层数
    'dim_feedforward': [256, 512, 1024],  # 前馈网络维度
    'dropout': [0.1, 0.2, 0.3],         # Dropout率
    'learning_rate': [1e-5, 5e-5, 1e-4], # 学习率
    'batch_size': [4, 8, 16],              # Batch大小
    'temperature': [0.3, 0.5, 1.0],      # 损失函数温度参数
    'top_k_weight': [2.0, 5.0, 10.0],   # Top-K样本权重
    'pairwise_weight': [1.0, 2.0, 5.0],  # 成对损失权重
}
```

**搜索策略**：
- 使用随机搜索或贝叶斯优化（如Optuna、Hyperopt）
- 优先搜索对性能影响大的超参数（如`d_model`、`learning_rate`、`temperature`）

### 6.3 模型解释性（理解激进策略）

**动机**：理解模型为什么做出某些预测，可以帮助你信任模型，并在必要时干预。

**实现方案**：
```python
def visualize_attention_weights(model, data_loader, device, num_samples=5):
    """可视化注意力权重"""
    model.eval()
    
    with torch.no_grad():
        for batch in data_loader:
            sequences = batch['sequences'].to(device)
            
            # 获取注意力权重
            scores, cross_attn_weights = model(sequences, return_attention=True)
            
            # 可视化股票间注意力权重
            # cross_attn_weights: [batch, nhead, num_stocks, num_stocks]
            for i in range(min(num_samples, cross_attn_weights.size(0))):
                attn = cross_attn_weights[i, 0]  # 取第一个头的注意力权重
                
                # 绘制热力图
                plt.figure(figsize=(10, 8))
                plt.imshow(attn.cpu().numpy(), cmap='hot', interpolation='nearest')
                plt.colorbar()
                plt.title(f'Sample {i+1} - Cross-Stock Attention Weights')
                plt.xlabel('Stock Index')
                plt.ylabel('Stock Index')
                plt.savefig(f'attention_visualization_sample_{i+1}.png')
                plt.close()
            
            if i >= num_samples:
                break
```

**激进风格适配**：
- 可视化注意力权重，理解模型如何关注不同股票
- 分析特征重要性，看看哪些特征对激进策略最重要

---

## 📈 第七部分：预期效果与风险评估

### 7.1 预期效果

| 改进项 | 预期效果 | 风险 |
|--------|----------|------|
| 代码错误修复 | 模型能正常运行 | 无 |
| 模型架构改进 | Top5选中股票的5日收益率提升15-25% | 模型复杂度增加，可能过拟合 |
| 训练策略优化 | 训练更稳定，收敛更快 | 激进学习率可能导致训练不稳定 |
| 特征工程增强 | 捕捉更多市场信号，提升预测能力 | 特征过多可能导致过拟合 |
| 风险控制方案 | 在可控风险下提升收益 | 可能错过一些高收益但高风险的机会 |

### 7.2 风险评估

**高风险项**：
1. **降低温度参数**：可能使模型过于自信，在极端市场情况下遭受巨大损失
2. **增加Top-K样本权重**：可能使模型过度关注少数高收益股票，忽略其他潜在机会
3. **使用激进学习率**：可能导致训练不稳定，模型无法收敛

**缓解措施**：
1. **添加不确定性估计**：在模型不确定性高时降低仓位
2. **严格止损机制**：一旦跌破阈值，立即清仓
3. **模型集成**：集成多个模型，降低单一模型的风险

---

## 🎯 第八部分：实施路线图

### 阶段1：紧急修复（1-2天）
- [ ] 修复model.py中的4处语法错误
- [ ] 修复train.py中的2处潜在问题
- [ ] 验证模型能正常运行

### 阶段2：模型架构改进（3-5天）
- [ ] 实现EnhancedCrossStockAttention模块
- [ ] 实现AggressiveFeatureAttention模块
- [ ] （可选）添加不确定性估计

### 阶段3：训练策略优化（2-3天）
- [ ] 实现AggressiveRankingLoss
- [ ] 添加动态学习率调度
- [ ] （可选）添加梯度累积

### 阶段4：特征工程增强（3-4天）
- [ ] 添加另类数据特征
- [ ] 改进标签构建（风险调整标签）

### 阶段5：风险控制方案（2-3天）
- [ ] 实现仓位管理模块
- [ ] 实现止损机制

### 阶段6：模型集成与超参数搜索（5-7天）
- [ ] 实现模型集成
- [ ] 进行超参数搜索

### 阶段7：模型解释性与部署（2-3天）
- [ ] 实现注意力可视化
- [ ] 部署模型到生产环境

---

## 📝 总结

本方案针对你的**激进风险偏好**，从代码修复、模型架构、训练策略、特征工程、风险控制等多个维度提出了全方位的优化建议。

**核心思路**：
1. **增强模型表达能力**：通过改进注意力机制、添加不确定性估计等
2. **优化训练目标**：通过改进损失函数，更关注高收益样本
3. **严格控制风险**：通过仓位管理、止损机制等，平衡激进与稳健

**预期效果**：
- 在可控风险下，模型Top5选中股票的5日收益率提升15-25%
- 模型更能适应极端市场环境，避免巨大损失

**下一步行动**：
1. 先修复代码错误，确保模型能正常运行
2. 逐步实现模型架构改进和训练策略优化
3. 添加风险控制方案，确保激进策略的稳健性

---

**祝你的模型在竞赛中取得好成绩！🏆**
