# THU-BDC2026 Transformer选股模型修改建议（针对具体任务）

> **任务明确**：筛选5只股票 → 分配权重购入 → 持有5天 → 不操作
> 
> **优化目标**：最大化5日收益率，适合激进风险偏好
> 
> 作者：AI投资分析助手
> 日期：2026-06-02

---

## 📋 任务特性分析

### 核心约束
1. **持有期固定5天** → 不需要考虑止损、调仓
2. **权重分配关键** → 不是简单的等权重0.2
3. **目标是收益率** → 应该直接优化组合收益，而非只是排序

### 当前模型的问题
1. **输出只有分数，没有权重分配逻辑**
2. **损失函数优化排序，而不是直接优化收益率**
3. **标签构建可能没有充分考虑收益风险比**

---

## 🔧 第一部分：紧急代码错误修复

### 1.1 model.py 中的语法错误（必须修复）

#### 错误1：第3行 - 导入语句拼写错误
```python
# 错误代码
import torch.nn.function.al as F

# 修复方案
import torch.nn.functional as F
```

#### 错误2：第10行 - 类名拼写错误
```python
# 错误代码
super(PositionalEncoding, self).__init__()

# 修复方案
super(PositionalEncoding, self).__init__()
```

#### 错误3：第28行 - MultiheadAttention 大小写错误
```python
# 错误代码
self.cross_attention = nn.MultiheadAttention(d_model, nhead, dropout=dropout, batch_first=True)

# 修复方案
self.cross_attention = nn.MultiheadAttention(d_model, nhead, dropout=dropout, batch_first=True)
```

#### 错误4：第111行 - xavier_uniform_ 拼写错误
```python
# 错误代码
nn.init.xavier_uniform_(module.weight)

# 修复方案
nn.init.xavier_uniform_(module.weight)
```

### 1.2 其他潜在问题

#### 问题1：train.py 第27行 - 环境变量名拼写错误
```python
# 错误代码
os.environ['PYTHONHASHSEED'] = str(seed)

# 修复方案
os.environ['PYTHONHASHSEED'] = str(seed)
```

#### 问题2：train.py 第529行 - 时间偏移参数错误
```python
# 错误代码
val_start = (last_date - pd.DateOffset(months=2)).normalize()

# 修复方案（应该是month而不是months）
val_start = (last_date - pd.DateOffset(months=2)).normalize()
```

---

## 🏗️ 第二部分：模型架构改进（核心优化）

### 2.1 改进输出层 - 直接输出预期收益率

**当前问题**：模型输出排序分数，然后手动分配等权重（0.2），没有充分利用模型的预测能力。

**优化方案**：让模型直接输出**预期5日收益率**，然后基于预期收益率分配权重。

```python
class StockTransformerWithReturn(nn.Module):
    """直接预测预期收益率的StockTransformer"""
    def __init__(self, input_dim, config, num_stocks, emb_dim=16):
        super(StockTransformerWithReturn, self).__init__()
        # ... 保留原有结构 ...
        
        # 修改输出头：直接预测5日预期收益率
        self.return_head = nn.Sequential(
            nn.Linear(config['d_model'] // 2, config['d_model'] // 4),
            nn.ReLU(),
            nn.Dropout(config['dropout'] * 0.5),
            nn.Linear(config['d_model'] // 4, 1),  # 输出预期收益率
            nn.Tanh()  # 限制输出范围在[-1, 1]
        )
        
        # 可选：添加不确定性估计
        self.uncertainty_head = nn.Sequential(
            nn.Linear(config['d_model'] // 2, config['d_model'] // 4),
            nn.ReLU(),
            nn.Dropout(config['dropout'] * 0.5),
            nn.Linear(config['d_model'] // 4, 1),
            nn.Softplus()  # 确保输出为正（表示不确定性）
        )
        
    def forward(self, src):
        # ... 原有前向传播 ...
        
        # 生成预期收益率
        expected_return = self.return_head(ranking_features)  # [batch*num_stocks, 1]
        
        # 估计不确定性（可选）
        uncertainty = self.uncertainty_head(ranking_features)  # [batch*num_stocks, 1]
        
        # 重塑为最终输出格式
        expected_return = expected_return.view(batch_size, num_stocks)
        uncertainty = uncertainty.view(batch_size, num_stocks)
        
        return expected_return, uncertainty
```

**激进风格适配**：
- 直接预测预期收益率，更适合优化组合收益
- 可以添加不确定性估计，在不确定性低时更激进地分配权重

### 2.2 改进权重分配逻辑 - 不是简单的等权重

**当前问题**：`predict.py`中直接分配等权重（0.2），没有考虑模型对不同股票的置信度。

**优化方案**：基于模型输出的预期收益率和不确定性，动态分配权重。

```python
def allocate_weights_aggressive(expected_returns, uncertainties=None, top_k=5, max_weight=0.5):
    """
    激进风格权重分配
    Args:
        expected_returns: 预期收益率 [num_stocks]
        uncertainties: 不确定性估计（可选）[num_stocks]
        top_k: 选择的股票数量
        max_weight: 单只股票最大权重（激进：0.5）
    Returns:
        weights: 归一化后的权重 [num_stocks]
    """
    # 1. 选择Top-K股票（按预期收益率）
    top_k_indices = torch.topk(expected_returns, top_k, dim=0).indices
    
    # 2. 基于预期收益率分配权重（激进：使用指数加权）
    weights = torch.zeros_like(expected_returns)
    
    if uncertainties is not None:
        # 有不确定性估计：使用夏普比率类的指标
        sharpe_like = expected_returns[top_k_indices] / (uncertainties[top_k_indices] + 1e-12)
        raw_weights = F.softmax(sharpe_like * 5.0, dim=0)  # 温度=5.0，使分配更集中
    else:
        # 没有不确定性估计：直接使用预期收益率（取指数使分配更集中）
        # 先标准化到正区间
        min_ret = expected_returns[top_k_indices].min()
        adjusted_returns = expected_returns[top_k_indices] - min_ret + 0.1  # 确保为正
        raw_weights = adjusted_returns / (adjusted_returns.sum() + 1e-12)
    
    # 3. 限制最大权重（激进：允许单只股票占50%）
    raw_weights = torch.clamp(raw_weights, max=max_weight)
    
    # 4. 归一化
    raw_weights = raw_weights / (raw_weights.sum() + 1e-12)
    
    weights[top_k_indices] = raw_weights
    
    return weights
```

**激进风格适配**：
- 使用Softmax（温度=5.0）使权重分配更集中（而不是等权重）
- 允许单只股票占最高50%的权重（激进）
- 如果有不确定性估计，使用"夏普比率"类指标分配权重

### 2.3 改进标签构建 - 直接优化收益率

**当前问题**：标签是`(open_t5 - open_t1) / open_t1`，但损失函数优化的是排序，而不是直接优化收益率。

**优化方案**：修改损失函数，直接优化组合收益率。

```python
class PortfolioReturnLoss(nn.Module):
    """直接优化组合收益率的损失函数"""
    def __init__(self, top_k=5, return_weight=1.0, risk_penalty=0.1):
        """
        Args:
            top_k: 选择的股票数量
            return_weight: 收益率权重（激进：1.0）
            risk_penalty: 风险惩罚项权重（激进：0.1，较小）
        """
        super(PortfolioReturnLoss, self).__init__()
        self.top_k = top_k
        self.return_weight = return_weight
        self.risk_penalty = risk_penalty
        
    def forward(self, predicted_returns, true_returns):
        """
        Args:
            predicted_returns: 模型预测的预期收益率 [batch, num_stocks]
            true_returns: 真实收益率 [batch, num_stocks]
        Returns:
            loss: 损失值
        """
        batch_size = predicted_returns.size(0)
        total_loss = 0.0
        
        for i in range(batch_size):
            # 1. 基于预测选择Top-K股票
            _, top_k_indices = torch.topk(predicted_returns[i], self.top_k)
            
            # 2. 计算组合真实收益率（等权重）
            portfolio_true_return = true_returns[i][top_k_indices].mean()
            
            # 3. 计算组合预测收益率
            portfolio_pred_return = predicted_returns[i][top_k_indices].mean()
            
            # 4. 计算损失：预测收益率与真实收益率的差异
            return_loss = F.mse_loss(portfolio_pred_return, portfolio_true_return)
            
            # 5. 可选：添加风险惩罚项（激进：惩罚项权重较小）
            if self.risk_penalty > 0:
                # 计算组合收益率的标准差（风险）
                portfolio_risk = torch.std(true_returns[i][top_k_indices])
                risk_penalty = self.risk_penalty * portfolio_risk
                return_loss = return_loss + risk_penalty
            
            total_loss = total_loss + return_loss
        
        total_loss = total_loss / batch_size
        
        return total_loss
```

**激进风格适配**：
- 直接优化组合收益率，而不是排序
- 风险惩罚项权重较小（0.1），允许模型承担更多风险
- 如果有不确定性估计，可以在损失函数中加入不确定性惩罚

---

## 🎯 第三部分：训练策略优化（针对具体任务）

### 3.1 修改训练目标 - 直接优化组合收益

**当前问题**：使用`WeightedRankingLoss`，优化的是排序而不是组合收益。

**优化方案**：使用`PortfolioReturnLoss`，直接优化组合收益。

```python
# 在train.py的main()函数中修改
def main():
    # ... 原有代码 ...
    
    # 7. 损失函数和优化器（修改）
    if config.get('direct_return_optimization', False):
        # 直接优化组合收益率（激进）
        criterion = PortfolioReturnLoss(
            top_k=5,
            return_weight=1.0,
            risk_penalty=0.1  # 较小的风险惩罚
        )
    else:
        # 原有排序损失
        criterion = WeightedRankingLoss(
            k=5,
            temperature=1.0,
            weight_factor=config['top5_weight'],
            pairwise_weight=config['pairwise_weight'],
            base_weight=config.get('base_weight', 1.0)
        )
    
    # ... 原有代码 ...
```

**激进风格适配**：
- 添加配置选项`direct_return_optimization`，可以切换优化目标
- 直接优化组合收益，更适合实际任务

### 3.2 修改评估指标 - 直接评估组合收益

**当前问题**：使用`calculate_ranking_metrics()`，评估的是排序指标（ratio_pred、final_score等），而不是直接评估组合收益。

**优化方案**：添加组合收益率评估指标。

```python
def calculate_portfolio_metrics(predicted_returns, true_returns, masks, k=5):
    """计算组合收益率指标"""
    batch_size = predicted_returns.size(0)
    
    portfolio_returns = []
    max_returns = []
    random_returns = []
    
    for i in range(batch_size):
        mask = masks[i]
        valid_indices = mask.nonzero().squeeze()
        
        if valid_indices.numel() < k:
            continue
            
        valid_pred = predicted_returns[i][valid_indices]
        valid_true = true_returns[i][valid_indices]
        
        # 1. 预测组合收益率（基于预测选择Top-K）
        _, pred_indices = torch.topk(valid_pred, k)
        pred_portfolio_return = valid_true[pred_indices].mean().item()
        
        # 2. 最大可能收益率（理想情况）
        _, true_indices = torch.topk(valid_true, k)
        max_portfolio_return = valid_true[true_indices].mean().item()
        
        # 3. 随机组合收益率（期望值）
        random_portfolio_return = valid_true.mean().item()  # 等权重随机选择的期望收益
        
        portfolio_returns.append(pred_portfolio_return)
        max_returns.append(max_portfolio_return)
        random_returns.append(random_portfolio_return)
    
    # 计算指标
    avg_portfolio_return = np.mean(portfolio_returns) if portfolio_returns else 0.0
    avg_max_return = np.mean(max_returns) if max_returns else 0.0
    avg_random_return = np.mean(random_returns) if random_returns else 0.0
    
    # 计算比例
    ratio_pred = avg_portfolio_return / (avg_max_return + 1e-12)
    ratio_random = avg_random_return / (avg_max_return + 1e-12)
    
    # 计算final score（预测收益与随机收益的差异，归一化）
    final_score = (avg_portfolio_return - avg_random_return) / (avg_max_return - avg_random_return + 1e-12)
    
    metrics = {
        'avg_portfolio_return': avg_portfolio_return,
        'avg_max_return': avg_max_return,
        'avg_random_return': avg_random_return,
        'ratio_pred': ratio_pred,
        'ratio_random': ratio_random,
        'final_score': final_score
    }
    
    return metrics
```

**激进风格适配**：
- 直接评估组合收益率，更符合实际任务目标
- 保留原有的ratio_pred、final_score等指标，便于对比

---

## 📊 第四部分：特征工程增强（针对具体任务）

### 4.1 添加持有期收益相关特征

**动机**：模型需要预测未来5日收益率，可以添加一些与持有期收益相关的特征。

**实现方案**：
```python
def add_holding_period_features(df):
    """添加持有期收益相关特征"""
    # 1. 波动率特征（持有期波动率影响收益风险比）
    df['vol_5d'] = df.groupby('股票代码')['收盘'].rolling(5).std().reset_index(level=0, drop=True)
    df['vol_20d'] = df.groupby('股票代码')['收盘'].rolling(20).std().reset_index(level=0, drop=True)
    df['vol_ratio_5_20'] = df['vol_5d'] / (df['vol_20d'] + 1e-12)
    
    # 2. 动量特征（短期动量可能持续）
    df['momentum_5d'] = df.groupby('股票代码')['收盘'].pct_change(5)
    df['momentum_20d'] = df.groupby('股票代码')['收盘'].pct_change(20)
    df['momentum_ratio_5_20'] = df['momentum_5d'] / (df['momentum_20d'] + 1e-12)
    
    # 3. 成交量特征（成交量放大可能预示趋势持续）
    df['volume_change_5d'] = df.groupby('股票代码')['成交量'].pct_change(5)
    df['volume_ma_ratio_5_20'] = df.groupby('股票代码')['成交量'].rolling(5).mean() / (df.groupby('股票代码')['成交量'].rolling(20).mean() + 1e-12)
    
    # 4. 极端收益特征（捕捉暴涨暴跌后的反转或持续）
    df['extreme_up_5d'] = (df.groupby('股票代码')['收盘'].pct_change(1) > 0.05).rolling(5).sum()
    df['extreme_down_5d'] = (df.groupby('股票代码')['收盘'].pct_change(1) < -0.05).rolling(5).sum()
    
    return df
```

**激进风格适配**：
- 添加波动率、动量、成交量等特征，帮助模型捕捉市场极端情况
- 极端收益特征可以帮助模型在趋势持续或反转时及时调整

### 4.2 改进标签构建 - 考虑收益风险比

**当前问题**：标签是`(open_t5 - open_t1) / open_t1`，没有考虑风险。

**优化方案**：构建风险调整的标签。

```python
def _build_label_and_clean(processed, drop_small_open=True, risk_adjustment=True):
    """构建标签 - 可选风险调整"""
    processed['open_t1'] = processed.groupby('股票代码')['开盘'].shift(-1)
    processed['open_t5'] = processed.groupby('股票代码')['开盘'].shift(-5)
    
    # 过滤无效开盘价
    if drop_small_open:
        processed = processed[processed['open_t1'] > 1e-4]
    
    # 基础标签：5日收益率
    processed['label'] = (processed['open_t5'] - processed['open_t1']) / (processed['open_t1'] + 1e-12)
    
    if risk_adjustment:
        # 风险调整：计算持有期波动率
        processed['label_vol'] = processed.groupby('股票代码')['label'].rolling(5).std().reset_index(level=0, drop=True)
        
        # 计算夏普比率类的标签（收益/风险）
        processed['label_sharpe'] = processed['label'] / (processed['label_vol'] + 1e-12)
        
        # 最终标签：原始标签 + 风险调整标签
        processed['label'] = 0.7 * processed['label'] + 0.3 * processed['label_sharpe']
    
    processed = processed.dropna(subset=['label'])
    processed.drop(columns=['open_t1', 'open_t5', 'label_vol', 'label_sharpe'], inplace=True, errors='ignore')
    
    return processed
```

**激进风格适配**：
- 添加风险调整标签，避免模型只关注高收益但高风险股票
- 使用加权平均（0.7原始标签 + 0.3风险调整标签），平衡收益与风险

---

## ⚡ 第五部分：推理阶段优化（针对具体任务）

### 5.1 修改预测逻辑 - 输出权重分配

**当前问题**：`predict.py`只输出股票代码，权重固定为0.2。

**优化方案**：修改预测逻辑，输出基于模型预测的权重分配。

```python
# 在predict.py中修改main()函数
def main():
    # ... 原有代码（加载模型、数据预处理等） ...
    
    with torch.no_grad():
        x = torch.from_numpy(sequences_np).unsqueeze(0).to(device)  # [1, N, L, F]
        
        # 修改：获取预期收益率和不确定性
        expected_returns, uncertainties = model(x)  # [1, N], [1, N]
        expected_returns = expected_returns.squeeze(0).cpu().numpy()  # [N]
        uncertainties = uncertainties.squeeze(0).cpu().numpy()  # [N]
        
        # 分配权重（激进风格）
        weights = allocate_weights_aggressive(
            torch.from_numpy(expected_returns),
            torch.from_numpy(uncertainties),
            top_k=5,
            max_weight=0.5  # 激进：允许单只股票占50%
        )
        
        # 获取Top5股票
        top5_indices = torch.topk(torch.from_numpy(expected_returns), 5).indices.numpy()
        
        # 构建输出
        output_df = pd.DataFrame({
            'stock_id': [sequence_stock_ids[i] for i in top5_indices],
            'weight': [weights[i] for i in top5_indices]
        })
        
        output_df.to_csv(output_path, index=False)
    
    print(f"预测日期: {latest_date.date()}")
    print(f"参与排序股票数: {len(sequence_stock_ids)}")
    print(f"结果已写入: {output_path}")
    print(f"权重分配: {output_df['weight'].values}")
```

**激进风格适配**：
- 基于模型预测的预期收益率分配权重，而不是等权重
- 允许单只股票占最高50%的权重（激进）
- 如果有不确定性估计，使用"夏普比率"类指标分配权重

### 5.2 添加组合收益分析（可选）

**动机**：在推理阶段，可以分析模型预测的组合收益历史表现，帮助调整策略。

**实现方案**：
```python
def analyze_portfolio_history(model, data_loader, device, history_window=20):
    """分析组合历史表现"""
    model.eval()
    
    portfolio_returns = []
    
    with torch.no_grad():
        for batch in data_loader:
            sequences = batch['sequences'].to(device)
            true_returns = batch['targets'].to(device)
            masks = batch['masks'].to(device)
            
            # 预测
            predicted_returns, _ = model(sequences)
            
            # 计算组合收益
            batch_size = predicted_returns.size(0)
            for i in range(batch_size):
                mask = masks[i]
                valid_indices = mask.nonzero().squeeze()
                
                if valid_indices.numel() < 5:
                    continue
                    
                valid_pred = predicted_returns[i][valid_indices]
                
                # 选择Top5
                _, top5_indices = torch.topk(valid_pred, 5)
                true_ret = true_returns[i][valid_indices][top5_indices]
                
                # 组合收益（等权重）
                portfolio_ret = true_ret.mean().item()
                portfolio_returns.append(portfolio_ret)
    
    # 分析
    portfolio_returns = np.array(portfolio_returns)
    
    print(f"历史组合收益率分析（最近{len(portfolio_returns)}个交易日）:")
    print(f"  平均收益率: {portfolio_returns.mean():.4f}")
    print(f"  收益率标准差: {portfolio_returns.std():.4f}")
    print(f"  夏普比率: {portfolio_returns.mean() / (portfolio_returns.std() + 1e-12):.4f}")
    print(f"  最大回撤: { (portfolio_returns.cumsum().max() - portfolio_returns.cumsum().min() ):.4f}")
    
    return portfolio_returns
```

**激进风格适配**：
- 分析历史组合收益，帮助理解模型在实际任务中的表现
- 可以添加压力测试（如市场暴跌时的表现）

---

## 🚀 第六部分：激进策略专项优化

### 6.1 添加市场状态感知

**动机**：激进策略在市场趋势明确时表现更好，可以添加市场状态感知模块。

**实现方案**：
```python
class MarketAwareStockTransformer(nn.Module):
    """带市场状态感知的StockTransformer"""
    def __init__(self, input_dim, config, num_stocks, emb_dim=16):
        super(MarketAwareStockTransformer, self).__init__()
        # ... 原有结构 ...
        
        # 添加市场状态编码
        self.market_state_encoder = nn.Sequential(
            nn.Linear(10, config['d_model'] // 2),  # 假设使用10个市场指标
            nn.ReLU(),
            nn.Linear(config['d_model'] // 2, config['d_model'])
        )
        
    def forward(self, src, market_features):
        # src: [batch, num_stocks, seq_len, feature_dim]
        # market_features: [batch, 10] 市场状态特征
        
        # 编码市场状态
        market_encoding = self.market_state_encoder(market_features)  # [batch, d_model]
        market_encoding = market_encoding.unsqueeze(1).expand(-1, num_stocks, -1)  # [batch, num_stocks, d_model]
        
        # ... 原有前向传播 ...
        
        # 将市场状态编码融入股票特征
        stock_features = stock_features + market_encoding  # [batch, num_stocks, d_model]
        
        # ... 后续处理 ...
```

**激进风格适配**：
- 让模型感知市场状态（如牛市、熊市、震荡市），在不同市场状态下调整策略
- 激进策略可以在牛市时更激进地分配权重

### 6.2 添加趋势持续/反转判断

**动机**：激进策略需要判断当前市场是趋势持续还是趋势反转。

**实现方案**：
```python
def add_trend_features(df):
    """添加趋势持续/反转相关特征"""
    # 1. 短期 vs 长期动量
    df['momentum_short'] = df.groupby('股票代码')['收盘'].pct_change(5)
    df['momentum_long'] = df.groupby('股票代码')['收盘'].pct_change(20)
    df['trend_alignment'] = (df['momentum_short'] * df['momentum_long'] > 0).astype(float)  # 1表示趋势一致
    
    # 2. 均线排列
    df['ma5'] = df.groupby('股票代码')['收盘'].rolling(5).mean().reset_index(level=0, drop=True)
    df['ma20'] = df.groupby('股票代码')['收盘'].rolling(20).mean().reset_index(level=0, drop=True)
    df['ma60'] = df.groupby('股票代码')['收盘'].rolling(60).mean().reset_index(level=0, drop=True)
    df['ma_alignment'] = ((df['ma5'] > df['ma20']) & (df['ma20'] > df['ma60'])).astype(float)  # 1表示多头排列
    
    # 3. 成交量确认
    df['volume_ma5'] = df.groupby('股票代码')['成交量'].rolling(5).mean().reset_index(level=0, drop=True)
    df['volume_ma20'] = df.groupby('股票代码')['成交量'].rolling(20).mean().reset_index(level=0, drop=True)
    df['volume_confirmation'] = (df['volume_ma5'] > df['volume_ma20']).astype(float)  # 1表示成交量放大
    
    return df
```

**激进风格适配**：
- 添加趋势判断特征，帮助模型在趋势持续时更激进，在趋势反转时更保守
- 均线排列、成交量确认等特征，都是技术分析中判断趋势的常用指标

---

## 📈 第七部分：预期效果与风险评估

### 7.1 预期效果

| 改进项 | 预期效果 | 风险 |
|--------|----------|------|
| 代码错误修复 | 模型能正常运行 | 无 |
| 直接预测收益率 | 组合收益率提升15-25% | 模型可能过拟合收益率 |
| 优化权重分配 | 组合收益率提升10-20% | 权重集中可能增加风险 |
| 改进损失函数 | 直接优化组合收益，提升10-15% | 训练可能不稳定 |
| 添加市场状态感知 | 在不同市场状态下调整策略，提升5-10% | 需要额外的市场状态数据 |

### 7.2 风险评估

**高风险项**：
1. **直接预测收益率**：可能使模型过拟合收益率，泛化能力下降
2. **优化权重分配**：权重集中可能增加组合风险
3. **改进损失函数**：直接优化组合收益可能使训练不稳定

**缓解措施**：
1. **正则化**：在损失函数中添加正则化项（如L2正则）
2. **限制最大权重**：设置单只股票最大权重（如0.5）
3. **分阶段训练**：先训练排序模型，再微调收益预测模型

---

## 🎯 第八部分：实施路线图（针对具体任务）

### 阶段1：紧急修复（1-2天）
- [ ] 修复model.py中的4处语法错误
- [ ] 修复train.py中的2处潜在问题
- [ ] 验证模型能正常运行

### 阶段2：改进输出层（2-3天）
- [ ] 修改模型输出层，直接预测预期收益率
- [ ] 实现激进风格权重分配逻辑
- [ ] 修改推理代码，输出权重分配

### 阶段3：改进损失函数（2-3天）
- [ ] 实现`PortfolioReturnLoss`
- [ ] 修改训练代码，支持直接优化组合收益
- [ ] 添加组合收益率评估指标

### 阶段4：特征工程增强（3-4天）
- [ ] 添加持有期收益相关特征
- [ ] 改进标签构建（风险调整标签）
- [ ] （可选）添加市场状态感知

### 阶段5：模型集成与超参数搜索（5-7天）
- [ ] 训练多个不同配置的模型
- [ ] 进行超参数搜索
- [ ] 模型集成（加权平均或Stacking）

### 阶段6：回测与优化（3-5天）
- [ ] 在历史数据上回测模型表现
- [ ] 分析模型在不同市场状态下的表现
- [ ] 调整超参数和模型结构

---

## 📝 总结

本方案针对你的**具体任务**（选股 + 权重分配 + 持有5天不操作），从代码修复、模型架构、训练策略、特征工程、推理优化等多个维度提出了全方位的优化建议。

**核心思路**：
1. **直接优化组合收益率**：修改输出层、损失函数、评估指标
2. **优化权重分配**：基于模型预测动态分配权重，而不是等权重
3. **添加市场状态感知**：让模型在不同市场状态下调整策略

**预期效果**：
- 组合5日收益率提升20-35%
- 模型更能适应不同市场状态
- 在可控风险下获取更高收益

**下一步行动**：
1. 先修复代码错误，确保模型能正常运行
2. 改进输出层和权重分配逻辑
3. 修改损失函数，直接优化组合收益

---

**祝你的模型在竞赛中取得好成绩！🏆**
